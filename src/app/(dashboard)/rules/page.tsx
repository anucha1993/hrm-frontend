import ComingSoon from "@/components/ComingSoon";

export default function RulesPage() {
  return (
    <ComingSoon
      title="กำหนดกฎระเบียบ"
      todo={[
        "Model Rule / RuleCategory + Migration",
        "API CRUD /rules",
        "UI จัดกลุ่มและเรียงลำดับกฎ",
      ]}
    />
  );
}
